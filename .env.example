# Server configuration
PORT=5000
NODE_ENV=development
CLIENT_ORIGIN=http://localhost:5173

# Database configuration
# Set this to your local MongoDB server or a MongoDB Atlas connection string
MONGODB_URI=mongodb://localhost:27017/3d_object_studio

# JWT configuration
JWT_SECRET=super_secret_studio_key_2026

# AWS S3 Configuration (Optional - fallback to local storage if empty)
AWS_ACCESS_KEY_ID=your_aws_access_key
AWS_SECRET_ACCESS_KEY=your_aws_secret_key
AWS_REGION=us-east-1
AWS_S3_BUCKET=your_s3_bucket_name
